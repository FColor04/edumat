import React from "react";
import styled from "styled-components";

const NotFoundPage = () => {
  return (
    <main>
      error
      <h1>404</h1>
    </main>
  );
};

export default NotFoundPage;
